pipeline {
    agent any
    stages {
        stage ('Compile Stage') {
            steps {
                sh 'mkdir -p build_pc && cd build_pc && cmake -DBUILD_TYPE=PC .. && make'
            }
        }
    }
}

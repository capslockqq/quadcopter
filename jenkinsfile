pipeline {
    agent any
    stages {
        stage ('Compile Stage') {
            steps {
                sh 'mkdir -p build_pc'
                sh 'cd build_pc'
                sh 'pwd'
                sh 'make'
            }
        }
    }
}
